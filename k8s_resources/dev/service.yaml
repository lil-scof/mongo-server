apiVersion: v1
kind: Service
metadata:
  name: mongodb-external
  namespace: scof-dev
spec:
  type: NodePort
  selector:
    app: mongodb
  ports:
    - name: mongodb
      protocol: TCP
      port: 27017
      targetPort: 27017
      nodePort: 30002